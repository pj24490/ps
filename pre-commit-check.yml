repos:
  - repo: local
    hooks:
      - id: pylint
        name: pylint (staged files only)
        entry: pylint
        language: system
        types: [python]
        args: ["--rcfile=.pylintrc"]
        stages: [pre-commit]